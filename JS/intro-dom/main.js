window.onload = () => { //This is to wait until the HTML charge fully so we can use it's elements
    const parrafo = document.getElementById("text")
    parrafo.innerHTML = '<li>Element 1</li><li>Element 2</li>'
}
